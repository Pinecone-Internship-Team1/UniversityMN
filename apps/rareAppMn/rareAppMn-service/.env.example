# Needed only for `bun run service:studio` (drizzle-kit studio against D1).
# Create a scoped API token at https://dash.cloudflare.com/profile/api-tokens
# (Account -> D1 -> Edit) — the `wrangler login` OAuth session doesn't work here.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_D1_TOKEN=
